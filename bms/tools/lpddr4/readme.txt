
DDRAPP_ORIGINAL.tar.gz -- original installer package
DDRAPP.tar.gz -- has installed package
interface_lib_source_code.tar.gz -- InterfaceLib.dll source code
env_dependencies.tar.gz -- DDRAPP runtime dependencies(maybe need)

Run bms and execute "lpddr4 -bp" and choose "0", then open DDRAPP and
click "jtag init --> load dll ctrl phy --> start ctrl". Finallly to read or write memory.
