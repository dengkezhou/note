匹配与替换

```
#使用替换操作在行首、行尾批量添加字符

#在行首添加# . 匹配一个字符， &表示已匹配的字符（不仅是.），
:%s/^/#
:%s/./# &   

#在行尾添加 “;”, .$用于匹配每行的最后一个字符， &表示已匹配的字符
:%s/.$/&;

#替换过程中指定行号
:n1,n2s
:/aaa/,/bbb/s    #指定开头单词为 aaa 的行 到开头为bbb的行 之间的行
```

